/*
times:(interval, offset, duration)
ops:(getCurrTime(), getReplTime(), update(queue), finish(job))
*/
var JobQueueScheduler = (function(){
  return function(times, ops) {
    this.invoke = function() {
      
    }
  }
})()
